name: atlas-controlplane

networks:
  atlas-controlplane-network:
    driver: bridge

volumes:
  prometheus_data:
  prometheus_targets:
  alertmanager_data:
  alertmanager_config:
  grafana_data:
  loki_data:
